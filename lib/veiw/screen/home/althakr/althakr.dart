import 'package:flutter/material.dart';


class Althakr extends StatelessWidget {
  const Althakr({super.key});

  @override
  Widget build(BuildContext context) {
   return const Column(
     children: [
       // MaterialButton(onPressed:() =>Get.to(page) ,child: ,)
     ],
   );
  }
}
